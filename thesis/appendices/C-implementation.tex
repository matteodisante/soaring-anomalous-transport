% Unnumbered: a numbered "Part I" would imply a "Part II" that does not exist. Still gets
% a TOC entry and a hyperref anchor (for \nameref/\pageref from the body) despite being
% starred.
\phantomsection
\addcontentsline{toc}{part}{Implementation and Computational Details}
\part*{Implementation and Computational Details}
\label{app:implementation}

% Independent numbering for this part: it mirrors the body, not the appendices, so its
% chapters must not continue the A/B letter sequence of Appendices A-B (that would read as
% "just more appendices of the same kind"), and, since the part itself is unnumbered above,
% a part-number prefix would be unexplained -- so these chapters get a plain, independent
% arabic sequence (1, 2, ...) instead, cascading automatically into \thesection/\thetable
% etc. Also revert the heading word from "Appendix" (set by \appendix, in effect since
% Appendix A) back to "Chapter", matching the body's own style.
\setcounter{chapter}{0}
\renewcommand{\thechapter}{\arabic{chapter}}
\makeatletter
\renewcommand{\@chapapp}{\chaptername}
\makeatother

\startcontents[impl]

\chapter{Overview}

This part collects the implementation- and computation-level detail behind the choices
made in the main text: the exact code module and script that produced each figure or
table, the configuration values in force, sample sizes and computation times, and the
status of each piece (implemented and tested, designed but not yet built, or planned). It
is organised as a document within the document: its own chapters mirror the body chapter
by chapter, so that a detail removed from the body sits in the corresponding chapter or
section here. It is not meant to be read start to end -- the body links to the relevant
place at the point where a detail was trimmed out of the text, via a cross-reference such
as ``(implementation details: Sec.~3.5.2)''.

\printcontents[impl]{l}{0}{\section*{Contents of this part}}

Table~\ref{tab:impl-repro} is a quick index of which script reproduces which
auto-generated figure or table (all paths relative to \texttt{scripts/reporting/}); the
chapters that follow go into the reasoning and numbers behind each one.

\begin{table}[htbp]
\centering\small
\begin{tabular}{@{}p{3.1cm}p{4.3cm}p{6.1cm}@{}}
\toprule
Figure/Table & Script & Notes \\
\midrule
Stats macros; Tables~\ref{tab:seasons-para},~\ref{tab:seasons-hang} &
  \path{generate_stats.py} &
  Reads committed \path{data/*/seasons_index.csv}; instant; part of the pre-commit hook. \\
Figure~\ref{fig:altnoise} &
  \path{generate_altitude_noise_figure.py} &
  Needs the raw IGC archive (env.\ \path{SOARING_PARA_DATA_ROOT}/\path{SOARING_DELTA_DATA_ROOT}). \\
Figures~\ref{fig:fixlevel}, \ref{fig:preproc}, \ref{fig:gaps}, \ref{fig:sampling} &
  \path{generate_preproc_figure.py} &
  One run produces all four (a shared full-census scan feeds three; fix-level cleaning
  uses a separate sampled pass). Needs the raw IGC archive. \\
\bottomrule
\end{tabular}
\caption{Script that regenerates each auto-generated thesis figure or table. Run as
\texttt{uv run python scripts/reporting/<name>}; only \texttt{generate\_stats.py} is wired
into \texttt{scripts/build\_docs.sh} (the two figure scripts need the external SSD and are
run manually).}
\label{tab:impl-repro}
\end{table}

\input{appendices/impl/C1-acquisition}
\input{appendices/impl/C2-dataset}
\input{appendices/impl/C3-next-steps}
