% =============================================================================
%  Anomalous transport in soaring flights -- Master's thesis (work in progress)
%
%  Measured values and figures are generated by scripts/reporting/.
%  Full cleaning + analyses + figures + PDF, from the repository root:
%    uv run python scripts/rebuild_thesis.py --clean --jobs 1
%  Layout-only build (does not refresh data): latexmk -pdf -halt-on-error main.tex
% =============================================================================
\documentclass[11pt,a4paper,twoside]{report}

% Omit volatile PDF metadata. Reproduction also depends on the build environment.
\ifdefined\pdfinfoomitdate\pdfinfoomitdate=1\fi
\ifdefined\pdftrailerid\pdftrailerid{}\fi

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
% Latin Modern (Computer Modern) text + math -- the classic LaTeX face, matching the
% reference document's typography.
\usepackage{lmodern}
\usepackage[english]{babel}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{enumitem}
\usepackage[a4paper,margin=2.7cm]{geometry}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{placeins} % Keep major-section evidence near its discussion.
\usepackage{float} % [H] placement, so a figure sits exactly where the text puts it
% Replaces plain LaTeX's \@makecaption, whose one-line-fit test measures the whole
% caption as a single unbroken hbox: past a few thousand characters (several figures
% here have long, data-justified captions) that pre-measurement itself overflows
% TeX's max dimension ("Dimension too large") even though the wrapped caption would
% render fine. `caption` has the same single-line check by default (to centre a
% caption short enough to need no wrapping); singlelinecheck=false disables it, since
% it is what overflows, not anything about the wrapped result.
\usepackage[singlelinecheck=false]{caption}
% Thesis caption style: small caption body so the long, data-justified captions recede, with
% a bold period-separated label ("Figure 2.2.").
\captionsetup{font=small, labelfont=bf, labelsep=period}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{longtable}
\usepackage{needspace}
\usepackage{multirow}
\usepackage{siunitx}
\usepackage{xcolor}
\usepackage{csquotes}
\usepackage{hyperref}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, calc, angles, quotes, positioning, decorations.pathreplacing}
% Table-of-contents formatting, including chapter-specific appendix numbers.
\usepackage{titletoc}
% TOC layout: the class reserves 1.5em for a chapter number, sized for a single
% digit/letter. The chapter-2 appendices are numbered 2.A and 2.B (see the note opening the
% appendix part), and that wider bold label overflows the box by ~3pt, colliding with
% the title. Widen the box for all chapter-level entries, so every chapter title in the
% TOC keeps starting at the same offset.
% (titletoc, just loaded, wraps \l@chapter in a dispatcher and stashes the class
% definition in \ttl@savel@chapter -- the patch must target whichever of the two
% actually holds the 1.5em.)
\usepackage{etoolbox}
\makeatletter
\ifpatchable{\l@chapter}{\setlength\@tempdima{1.5em}}
  {\patchcmd{\l@chapter}{\setlength\@tempdima{1.5em}}{\setlength\@tempdima{2.1em}}{}{}}
  {\patchcmd{\ttl@savel@chapter}{\setlength\@tempdima{1.5em}}{\setlength\@tempdima{2.1em}}
    {}{\GenericWarning{}{main.tex: TOC chapter-number box not widened (patch failed)}}}
% Same story one level down: section entries "2.B.1"-"2.B.4" of the geodesy appendix
% overflow the class's 2.3em section-number box; widen it for all section entries.
\ifpatchable{\l@section}{{2.3em}}
  {\patchcmd{\l@section}{{2.3em}}{{2.7em}}{}{}}
  {\patchcmd{\ttl@savel@section}{{2.3em}}{{2.7em}}
    {}{\GenericWarning{}{main.tex: TOC section-number box not widened (patch failed)}}}
\makeatother

% ---- Running headers (book style, two-sided). On an even (left) page the chapter
% ---- title sits flush against the outer (left) edge; on an odd (right) page the
% ---- current section title sits flush against the outer (right) edge; a rule runs
% ---- under both, and the page number is in the outer footer. The class is `twoside`
% ---- so the two sides can differ; `report` is `openany`, so no blank pages are added.
\usepackage{fancyhdr}
\setlength{\headheight}{14pt}   % room for the header + rule (silences fancyhdr's warning)
\makeatletter
% "Chapter N. Title" on verso, "Appendix A. Title" in the back matter (\@chapapp tracks
% the \appendix switch); mixed case, not the class default's all-caps running head.
\renewcommand{\chaptermark}[1]{\markboth{\@chapapp\ \thechapter.\ #1}{}}
\makeatother
\renewcommand{\sectionmark}[1]{\markright{\thesection.\ #1}}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\itshape\nouppercase{\leftmark}}   % even (left) page: chapter, outer/left
\fancyhead[RO]{\itshape\nouppercase{\rightmark}}  % odd (right) page: section, outer/right
\fancyfoot[LE,RO]{\thepage}                         % page number on the outer edge
\renewcommand{\headrulewidth}{0.4pt}
% Chapter-opening pages (class style `plain`): no running head, page number only.
\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyfoot[LE,RO]{\thepage}%
  \renewcommand{\headrulewidth}{0pt}%
}

\sisetup{group-separator={,}, group-minimum-digits=4}

% Float placement: with several large figures (and a few tall, long-captioned ones),
% LaTeX's default fractions were too conservative -- a figure that didn't quite fit
% "here" but wasn't judged to fill a whole page either was routinely deferred to its
% own near-empty page. Loosening these lets a big figure share a page with text
% whenever there is room, rather than defaulting to an isolated float page.
\renewcommand{\topfraction}{0.85}
\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.15}
\renewcommand{\floatpagefraction}{0.75}

% Palette shared with the matplotlib figures (soaring.analysis), for visual consistency.
\definecolor{figblue}{HTML}{3477A8}
\definecolor{figred}{HTML}{B5482A}
\definecolor{figgreen}{HTML}{4E8A5B}
% Flight-phase colours, shared with PHASE_COLORS in src/soaring/reporting/style.py.
% They are deliberately not figblue/figred: blue and terracotta mean paraglider and
% hang glider throughout, and a phase is a different variable.
\definecolor{phasetransition}{HTML}{2E7D8A}
\definecolor{phasesearch}{HTML}{A9629E}
\definecolor{phaseclimb}{HTML}{4E8A5B}

% ---- Diagram typography, shared with the plotted figures --------------------
% The measured figures are drawn by Matplotlib in DejaVu Sans at 9pt on a 6.1-inch
% canvas (src/soaring/reporting/style.py), which is this document's text width, so
% they print at that size. Loading DejaVuSans redefines only the sans family: the
% body text stays Latin Modern, while \figfont switches a hand-drawn schematic to
% the plotting face, mathematics included. The styles below carry the rest of what
% those figures share -- hairline axes, pale grid, grey annotation, left-set panel
% titles -- so a schematic and a measurement look like one set of figures.
\usepackage[scaled=0.90]{DejaVuSans}
\usepackage{sansmath}
\newcommand{\figfont}{\sffamily\sansmath}
\tikzset{
  schematic/.style={
    >=Latex,
    line width=0.9pt,
    font=\small\figfont,
    every node/.append style={font=\small\figfont},
  },
  fig axis/.style={draw=black!55, line width=0.6pt},
  fig rule/.style={draw=black!35, line width=0.5pt},
  fig grid/.style={draw=black!14, line width=0.4pt},
  fig tick/.style={font=\footnotesize\figfont, text=black!55},
  fig note/.style={font=\footnotesize\figfont, text=black!55},
  fig label/.style={font=\small\figfont},
  panel title/.style={font=\small\figfont, anchor=west, inner sep=0pt},
}

% ---- Revision markup (July 2026 review pass on main_revision.pdf). Two channels:
% ----   \rev{...}  (blue)   -- text added or rewritten in response to a reviewer annotation;
% ----   \flow{...} (orange) -- readability-only rewording (sentence rhythm, paragraphing),
% ----                          not requested by any specific annotation.
% ---- Flip \revmodefalse for the clean (all-black) final version; the commands then
% ---- pass their argument through unchanged.
\definecolor{revblue}{HTML}{1565C0}
\definecolor{revorange}{HTML}{E07B00}
\definecolor{errorred}{rgb}{1,0,0}
\newif\ifrevmode
\revmodefalse
\newcommand{\rev}[1]{\ifrevmode\textcolor{revblue}{#1}\else#1\fi}
\newcommand{\flow}[1]{\ifrevmode\textcolor{revorange}{#1}\else#1\fi}
\newcommand{\err}[1]{\ifrevmode\textcolor{errorred}{#1}\else#1\fi}
% Block forms of \rev/\flow for material that spans sectioning commands, floats or lists
% (a plain \textcolor argument must not contain \section&c.).
\newenvironment{revblock}{\ifrevmode\color{revblue}\fi}{}
\newenvironment{flowblock}{\ifrevmode\color{revorange}\fi}{}

% Subtle, navigable hyperlinks in the figure palette (dark blue for internal refs and URLs,
% dark green for citations), plus PDF metadata and numbered bookmarks. Set after the colours
% are defined.
\hypersetup{
  colorlinks=true,
  linkcolor=figblue,
  citecolor=figgreen,
  urlcolor=figblue,
  linktoc=all,
  bookmarksnumbered=true,
  pdftitle={Anomalous Transport in Soaring Flights},
  pdfauthor={Matteo Di Sante},
  pdfsubject={MSc thesis --- Physics of Complex Systems},
}


% Traditional BibTeX (portable: no biber required). latexmk runs bibtex automatically.
\usepackage{cite}

% Headline numbers (\StatParaTotalFlights, \StatHangTotalFlights, ...) generated from the data.
\input{generated/stats}
% Census-scan numbers (\StatScanParaTracks, \StatScanParaNoBaroPct, ...) generated from the
% cached full-census track scans, so the prose cannot drift from the scan that produced it.
\input{generated/census}
% Current measurement products; regenerated by the complete workflow.
\input{generated/msd}
\input{generated/altitude_noise}
\input{generated/pipeline_census}
\input{generated/verify}
\input{generated/audit}
\input{generated/prelim}
\input{generated/terrain}
\input{generated/dataset_stats}
% Kinematic-isotropy numbers (\StatKinParaVelocityMedian, ...): the raw component second-moment
% ratio read off velocity and acceleration as well as displacement, by discipline, terrain
% and equipment class.
\IfFileExists{generated/kinematic_isotropy.tex}{\input{generated/kinematic_isotropy}}{}
% Fresh, explicitly sampled 10--10000 s revision diagnostics and provenance.
\input{generated/ch3_revision}
\input{generated/duration_equipment}
\input{generated/snapshot_status}
% Fitting and decoding counts used by the generated Chapter 4 tables.
\input{generated/segmentation}

\title{Anomalous Transport in Soaring: Collective Strategies and Intermittent Search Models}
\author{Matteo Di Sante}

\begin{document}

% ----------------------------- Title page ------------------------------------
\begin{titlepage}
\centering
{\scshape\large Università di Pisa \\ Department of Physics\par}
{\scshape Master's Degree in Physics\par}
\vfill
{\Huge\bfseries Anomalous Transport in Soaring Flights\par}
\vspace{0.8em}
{\LARGE Collective Strategies and Intermittent Search Models\par}
\vfill
\begin{minipage}[t]{0.45\textwidth}
  \raggedright\large
  \textit{Candidate}\\
  Matteo Di Sante\par
\end{minipage}\hfill
\begin{minipage}[t]{0.45\textwidth}
  \raggedleft\large
  \textit{Supervisors}\\
  Prof.\ Michael Benzaquen\\
  Dr Alexandre Darmon\\[0.6em]
  \textit{Internal tutor (Pisa)}\\
  Prof.\ Riccardo Mannella\par
\end{minipage}
\vfill
{\large Academic year 2025--2026\par}
\vspace{1em}
{\small Working draft --- last modified \today.\\
Sources and code: \url{https://github.com/matteodisante/soaring-anomalous-transport}\par}
\end{titlepage}

% Front matter (abstract, contents) in roman numerals; the body switches to arabic,
% restarting at 1, just before the first chapter.
\pagenumbering{roman}

% ----------------------------- Abstract --------------------------------------
\begin{abstract}
\noindent
Soaring flight couples persistent horizontal motion to the search for atmospheric lift
and the management of altitude. This thesis investigates which stochastic descriptions
can account for that motion using paraglider and hang-glider records from the French
free-flight federation. It combines empirical observables, probabilistic flight-phase
segmentation and a programme of mathematical modelling and Monte Carlo validation.

The data chapter documents recorder channels, cleaning and the limits of the retained
trajectories. Global transport is then examined through displacement moments and quantiles,
constant-velocity controls, regional anisotropy and velocity memory at several temporal
resolutions. A fresh subset analysis over 10--10,000 seconds shows why effective exponents,
changes of distribution shape and finite observation windows must be distinguished.
Principal-axis analysis describes regional geometry without identifying wind or removing
environmental effects. Competing stochastic models are assessed through explicit
predictions, with the scope of each exclusion stated.

The segmentation chapter presents the current probabilistic decoder, readable flight
examples and quantitative diagnostics. Independent manual annotation remains necessary
before accuracy can be claimed.
\ifnum\StatSnapshotCurrent=0
The numerical figures are awaiting complete regeneration after the cleaning revision.
\fi
Subsequent work will
measure observables conditioned on flight phase, compare solitary and group flight,
construct and simulate a physically constrained stochastic model, and investigate thermal
landscapes and route optimisation.
\end{abstract}

\tableofcontents

\clearpage
\pagenumbering{arabic}

% Source filenames retain their historical numbering: 03-dataset is Chapter 2,
% 04-global-transport is Chapter 3, and 05-flight-phases is Chapter 4.
% Acquisition is described in the dataset chapter itself.
\input{sections/01-introduction}
\input{sections/03-dataset}
\input{sections/04-global-transport}
\input{sections/05-flight-phases}

\bibliographystyle{unsrt}
\bibliography{references}

\appendix
% Both retained appendices support Chapter 2. Number them in order of first
% citation: spectral estimation (2.A), then geodetic coordinates (2.B).
\renewcommand{\thechapter}{2.\Alph{chapter}}
% A separate divider states this numbering convention before the appendices.
\clearpage
\thispagestyle{plain}
\phantomsection
\addcontentsline{toc}{part}{Appendices}
\null\vfil
{\centering\normalfont\Huge\bfseries Appendices\par}
\vspace{2\baselineskip}
\begin{flowblock}
\begin{center}
\begin{minipage}{0.78\textwidth}
The appendices supply the spectral and geodetic definitions used in Chapter~2.
They are numbered 2.A and 2.B in their order of first citation.
\end{minipage}
\end{center}
\end{flowblock}
\vfil\vfil
\newpage
\input{appendices/B-psd}
\input{appendices/geodesy}

\end{document}
