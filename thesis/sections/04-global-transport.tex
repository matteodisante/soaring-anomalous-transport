\chapter{Global transport}
\label{ch:global}

\begin{revblock}
This chapter measures the transport of the retained ensemble without segmenting a single
flight. What can be established that way is a \emph{class} of transport: how far a wing
gets in a given time, how that growth is distributed, how long the motion remembers its
direction, and how many distinct regimes the answer needs. What cannot be established is
which flight behaviour produces it, and that is Chapter~\ref{ch:phases}.

The measurements are made on the analysis ensemble assembled in Chapter~\ref{ch:dataset}:
\num{\StatMsdParaFlights} paraglider and \num{\StatMsdHangFlights} hang-glider flights,
holding \num{\StatMsdParaSegments} and \num{\StatMsdHangSegments} retained segments. Every
statistic below is computed within a retained segment and never across the gap that ends
one (Sec.~\ref{sec:uniform}) \impldetails{impl:global}.

Two constraints from Sec.~\ref{sec:prelim} hold throughout. The horizontal components are
anisotropic --- the ratio of their mean squares sits at \StatPrelimParaIsoRatioMedian\ for
paragliders and \StatPrelimHangIsoRatioMedian\ for hang gliders, never touching unity ---
so the two marginals are measured separately and a pooled one-dimensional marginal never
substitutes for the two-dimensional propagator. And the discipline is heterogeneous: read
against the pooled ensemble MSD, the typical wing class departs by
\SI{\StatPrelimParaClassTypicalDeparturePct}{\percent} and the typical orographic group by
\SI{\StatPrelimParaGroupTypicalDeparturePct}{\percent}, against
\SI{\StatPrelimParaSeasonTypicalDeparturePct}{\percent} across seasons. Wing class and
terrain are relevant strata and season is not, so every exponent below is reported by
stratum as well as pooled, and the two disciplines are never pooled into each other.

Over \SIrange{\StatVarParaFitMinS}{\StatVarParaFitMaxS}{\second}, on an estimator that
annihilates any polynomial trend in the trajectory, cross-country soaring gives
$\alpha_2=\StatVarParaAlphaOrderTwo\pm\StatVarParaAlphaOrderTwoErr$ for paragliders and
$\StatVarHangAlphaOrderTwo\pm\StatVarHangAlphaOrderTwoErr$ for hang gliders. It is not the
Lévy walk this thesis was framed around: the spectrum of moments is straight, $\nu(q)$
moving by \StatShapeParaNuSpread\ and \StatShapeHangNuSpread\ across the range of $q$ read.
It is not Gaussian either: the non-Gaussian parameter is
$\StatShapeParaNonGaussMedian$ for paragliders and $\StatShapeHangNonGaussMedian$ for hang
gliders in the median, rising to an interior maximum near
\SI{\StatShapeParaNonGaussPeakS}{\second}, so one exponent governing every moment states
how the increment distribution scales and nothing about its shape.
\end{revblock}

\section{Displacement from take-off measures the launch geometry}
\label{sec:obs-global}
\begin{revblock}
Every flight starts at the origin, $\mathbf{r}(0)=\mathbf{0}$, so the position after an
elapsed time $t$ and the displacement from take-off are the same vector. Its mean square
admits two averages that answer different questions. The ensemble average
\begin{equation}
  \mathrm{MSD}(t)=\big\langle\,|\mathbf{r}(t)|^2\big\rangle ,
  \label{eq:ea-msd}
\end{equation}
taken over flights at a fixed elapsed time, fixes the transport regime through
$\mathrm{MSD}(t)\sim t^{\alpha}$ --- $\alpha=1$ Brownian, $\alpha>1$ super-diffusive,
$\alpha<1$ sub-diffusive, $H=\alpha/2$ --- and is the observable the anomalous-transport
question is normally posed with. The time average
\begin{equation}
  \overline{\delta^2(\tau)}
  =\big\langle\,|\mathbf{r}(t_0+\tau)-\mathbf{r}(t_0)|^2\big\rangle_{t_0} ,
  \label{eq:ta-msd}
\end{equation}
taken over starting times $t_0$ inside one record, answers how far a wing travels in a
window of duration $\tau$ placed anywhere in its flight. The ensemble average is
synchronised at take-off, so a feature it carries at a fixed elapsed time is a property of
that common origin; the time average slides its window with no privileged origin, so a
feature the first shows and the second does not belongs to the origin rather than the
motion.

Both are computed on the analysis ensemble of Sec.~\ref{sec:prelim}
(\num{\StatMsdParaFlights} paraglider, \num{\StatMsdHangFlights} hang-glider flights,
\num{\StatMsdParaSegments} and \num{\StatMsdHangSegments} retained segments), within a
retained segment and never across the gap that ends one (Sec.~\ref{sec:uniform}), with a
flight answering a lag only when it holds a fix within half its own native interval of it
\impldetails{impl:global}. Figure~\ref{fig:msd} shows both estimators.

\paragraph{The ensemble curve is not a power law.}
Over the range each estimator is fitted on, the ensemble curve departs from its own fitted
line by \SI{\StatAuditParaEaResidPct}{\percent} rms for paragliders and
\SI{\StatAuditHangEaResidPct}{\percent} for hang gliders, against
\SI{\StatAuditParaTaResidPct}{\percent} and \SI{\StatAuditHangTaResidPct}{\percent} for the
time average. That residual exceeds the standard error on each point by
\num{\StatAuditParaEaResidNoiseRatio} and \num{\StatAuditHangEaResidNoiseRatio} on the two
ensemble curves, against \num{\StatAuditParaTaResidNoiseRatio} and
\num{\StatAuditHangTaResidNoiseRatio} on the time averages: a departure exceeding the error
on each point many times over is a shape, not scatter about a power law
\impldetails{impl:global}.

\paragraph{The shape is an arch above the ballistic value.}
The ensemble local slope falls to a minimum of \StatAuditParaEaSlopeMin\ near
\SI{\StatAuditParaEaSlopeMinAt}{\second}, rises to \StatAuditParaEaSlopeMax\ near
\SI{\StatAuditParaEaSlopeMaxAt}{\second}, and settles back towards $2$; hang gliders do the
same between \StatAuditHangEaSlopeMin\ and \StatAuditHangEaSlopeMax. The maximum lies
\emph{above} the ballistic value, which no sum of power laws with positive coefficients and
exponents at or below $2$ can produce: for $f=\sum_i a_i t^{\alpha_i}$ with $a_i>0$,
\begin{equation}
  \frac{\mathrm{d}\log f}{\mathrm{d}\log t}
  =\frac{\sum_i a_i\alpha_i t^{\alpha_i}}{\sum_i a_i t^{\alpha_i}}
  \label{eq:weighted-slope}
\end{equation}
is a weighted mean of the $\alpha_i$ and cannot exceed the largest of them, so a slope
above $2$ requires the ballistic component to switch on at a finite time rather than to be
present from $t=0$. The arch is larger than the estimator's own uncertainty
\impldetails{impl:global}.

\paragraph{Three controls rule out an artefact.}
A changing population, survivorship, and a shared heading could each produce the same arch
without it belonging to the motion. Splitting by native cadence removes any drift from a
growing population: across the \StatAuditParaCadenceCohorts\ paraglider classes the local
slope spreads by only \StatAuditParaCadenceSpread\ against a swing of
\StatAuditParaEaSlopeSwing. Fixed-duration cohorts remove survivorship: the local slope
spreads across cohorts by \StatAuditParaDurationSpread\ and \StatAuditHangDurationSpread,
where a synthetic ensemble with only the \emph{speed} correlated to duration reproduces
neither feature \impldetails{impl:global}. And a shared heading is bounded by the part of
the mean square the mean vector carries, which never exceeds
\SI{\StatAuditParaDriftPct}{\percent} for paragliders and \SI{\StatAuditHangDriftPct}{\percent}
for hang gliders. The flights do not share a heading; they share a starting point.

\paragraph{The origin of the crossover.}
A wing does not leave its launch site immediately: it climbs out, often circling close to
it, and only then commits to a course. Until it does, its distance from take-off is bounded
by the local soaring area however fast it is flying, which depresses the local slope at
intermediate lags; once the population has committed, the distance grows with the flown
speed, which returns the slope towards $2$. Because commitment happens at a spread of times
rather than at $t=0$, the transition overshoots. Taking the first crossing of
\SI{\StatAuditParaDepartureRadiusKm}{\kilo\metre} from take-off as the departure onto the
cross-country leg --- well inside the median flown path of
\SI{\StatPipeParaMedianPathKm}{\kilo\metre} --- \SI{\StatAuditParaDepartureLeavePct}{\percent}
of paraglider flights depart, with a median departure time of
\SI{\StatAuditParaDepartureMedianS}{\second}, against a slope peak at
\SI{\StatAuditParaEaSlopeMaxAt}{\second}; for hang gliders
\SI{\StatAuditHangDepartureLeavePct}{\percent} depart at a median of
\SI{\StatAuditHangDepartureMedianS}{\second} against a peak at
\SI{\StatAuditHangEaSlopeMaxAt}{\second}. The radius is chosen rather than fitted, so the
agreement corroborates the account rather than measuring it. The ensemble MSD about
take-off is therefore a statement about launch geometry over the decade where the overshoot
sits, and an exponent fitted across that crossover --- $\alpha_{\mathrm{EA}}=\StatMsdParaAlpha$
and \StatMsdHangAlpha\ over \SIrange{\StatMsdParaFitMinS}{\StatMsdParaFitMaxS}{\second} ---
is a chord across a bend rather than a growth law the motion obeys.

\paragraph{The time average shows no arch.}
Panel~(b) is Eq.~\eqref{eq:ta-msd} on the same ensemble. Over
\SIrange{\StatMsdTaParaFitMinS}{\StatMsdTaParaFitMaxS}{\second} and
\SIrange{\StatMsdTaHangFitMinS}{\StatMsdTaHangFitMaxS}{\second} it gives
$\alpha_{\mathrm{TA}}=\StatMsdTaParaAlpha$ ($H=\StatMsdTaParaHurst$) for paragliders and
$\StatMsdTaHangAlpha$ ($H=\StatMsdTaHangHurst$) for hang gliders, both above Brownian and
below ballistic, with a smaller residual, and a local slope that reaches its minimum at
\SI{\StatAuditParaTaSlopeMinAt}{\second} and \SI{\StatAuditHangTaSlopeMinAt}{\second} and
rises from there, with no interior maximum at all. Since the two averages differ only in
what they average over, the crossover is a property of the common origin, confirmed by its
absence from panel~(b); both exponents are reported as \emph{effective} exponents over a
stated range.

\paragraph{The gap between the two averages is not evidence of ageing.}
Fitted over their respective ranges the ensemble slope exceeds the time-averaged exponent by
\num{\StatMsdParaAlphaGap} for paragliders and \num{\StatMsdHangAlphaGap} for hang gliders, a
gap of the kind that signals a non-ergodic, ageing process~\cite{he2008}. Reading it that way
here presupposes both curves are exponents, and the ensemble figure is a chord across a curve
whose local slope covers \StatAuditParaEaSlopeSwing, so changing the fitted range moves it by
more than the gap. What the measurement supports is weaker: displacement from take-off grows
faster, over the upper part of its range, than displacement over a window of the same
duration placed anywhere in the flight. Separating ageing from launch geometry needs the
phase decomposition of Sec.~\ref{sec:segmentation} and the ergodicity-breaking parameter of
Appendix~\ref{app:ctrw}, neither of which needs a curve to be a power law.

\paragraph{What the two estimators return.}
The time average gives an effective exponent on a curve close to a single power law. The
ensemble average gives a timescale, not an exponent: displacement from take-off crosses over
near \SI{\StatAuditParaEaSlopeMaxAt}{\second} for paragliders and
\SI{\StatAuditHangEaSlopeMaxAt}{\second} for hang gliders, where the population leaves its
launch area, and no single power law spans that crossover. Neither curve carries a
confidence interval here: a bootstrap over flights would treat the archive's records as
independent, which they are not, and a curve that is a systematic arch has no single exponent
for a sampling error to be an error \emph{of}. The resampling unit and the range dependence
are measured in Sec.~\ref{sec:transport-measure}, on the estimator that replaces this one.

Below the fitted range the curves carry two pipeline artefacts rather than flying ones: a
coverage rule meeting the integer lag grid, and the smoothing evaluated off-centre at a
segment's first samples, which moves the curve by up to \SI{\StatEdgeParaShortPct}{\percent}
below \SI{\StatEdgeParaShortLagS}{\second} on the \SI{\StatEdgeParaSamplePct}{\percent} of
fixes it touches \impldetails{impl:global}. Above it, the ensemble thins to a
survivorship-selected minority, holding only \num{\StatAuditParaFlightsWideLag} flights at
\SI{\StatAuditParaFlightsWideLagS}{\second}. Nothing outside the fitted range in
Figure~\ref{fig:msd} is read as motion.

A displacement statistic on this archive is contaminated by the common origin just
diagnosed; by each flight's own course, which does not simply inflate it, since
\SI{\StatVarParaClosedPct}{\percent} of retained paraglider flights fly a closed triangle
that saturates displacement at a scoring-rule timescale rather than a flying one; and by the
heterogeneity of Sec.~\ref{sec:prelim}, which lets a superposition of ordinary processes
with different persistence times imitate a single anomalous one. None of the three is
repaired by subtracting an estimate of it; the next section meets the specification by
filtering instead.
\end{revblock}

\begin{figure}[htbp]
  \centering
  \IfFileExists{generated/msd.pdf}{\includegraphics[width=\linewidth]{generated/msd}}{%
    \fbox{\parbox{0.8\linewidth}{\centering\small Figure generated locally by
    \texttt{scripts/reporting/generate\_msd\_figure.py}.}}}
  \caption{\rev{Mean-squared displacement of the retained ensemble, by two averages.
  (a)~Ensemble MSD against elapsed flight time, log--log, fitted line and range shaded,
  \SIrange{10}{90}{\percent} band across flights. (b)~Time-averaged MSD, cut at half a
  segment's length. (c)~Local logarithmic slope of both, ballistic and diffusive values
  marked. (d)~Flights contributing at each lag, which sets the upper end of each fit
  range.}}
  \label{fig:msd}
\end{figure}

\section{The filtered variation}
\label{sec:variations}
\begin{revblock}
The displacement statistic leaves a specification: a quantity measured from no common
origin, requiring no estimate of the flown course, and not turned into a single anomalous
law by a mixture of populations. The first two are met by construction, the third by
measurement below.

\paragraph{Filtering rather than subtracting.}
The course could be subtracted rather than filtered, but subtraction fails here: the
natural estimate $[\mathbf{r}(T)-\mathbf{r}(0)]/T$ is built from the record's own
endpoints, and subtracting it turns the trajectory into a bridge whose increments must sum
to zero by construction. On synthetic fractional Brownian motion of known exponent the
operation returns a value well below the truth at every lag, worst towards the record's own
length \impldetails{impl:global}.

\paragraph{The estimator.}
A finite difference removes a trend without estimating it, because it annihilates the trend
identically. Writing $A_p$ for the difference of order $p$ taken at spacing $\Delta$,
\begin{equation}
  A_p\,\mathbf{r}(t;\Delta)
  =\sum_{k=0}^{p}(-1)^{k}\binom{p}{k}\,\mathbf{r}\bigl(t+(p-k)\Delta\bigr),
  \label{eq:filter}
\end{equation}
the operator vanishes identically on every polynomial trajectory of degree $p-1$, and the
\emph{filtered variation} is its mean square,
\begin{equation}
  V_p(\Delta)=\Bigl\langle\,\bigl|A_p\,\mathbf{r}(t;\Delta)\bigr|^{2}\Bigr\rangle
  \;\sim\;\Delta^{\alpha_p},
  \label{eq:variation}
\end{equation}
averaged over every admissible $t$ and flight and summed over the two horizontal
components, which is what makes it invariant under a rotation of the frame: an anisotropic
ensemble must not return a different exponent for having been measured with north somewhere
else. The propagator of Sec.~\ref{sec:transport-propagator} does the opposite and keeps the
components apart, because the ensemble is anisotropic (Sec.~\ref{sec:prelim}) and a
one-dimensional marginal is exactly what it reads; the two are different statistics of the
same trajectories rather than one of them pooled. For a self-similar process with stationary increments $\alpha_p=2H$ at every
order, with only the prefactor depending on $p$: $p=1$ is the plain increment and is blind
to the origin, $p=2$ to a constant velocity, $p=3$ to a constant acceleration, so no drift
is estimated at any order and none can be estimated badly. The three orders are not
successive refinements of one number, and are not expected to agree: $\alpha_1$ is the
exponent of displacement with the flown course still inside it, $\alpha_2$ is what that
displacement does once a mean velocity --- the course --- is set aside, and a course that is
no part of the wandering still adds to how far a wing gets, so the two answer different
questions even when both are read off the same trajectories. A difference of order $p$ spans
$p\Delta$ of record, so each order costs coverage at the long end of the grid and the scan
stops at $p=3$.

The scan over orders is itself a measurement. The difference $\alpha_1-\alpha_2$ is the part
of the apparent exponent that was the flown course, read off directly rather than modelled,
and the agreement between $\alpha_2$ and $\alpha_3$ is the statement that nothing
polynomial is left for a higher difference to find. On synthetic data with a constant
course imposed on a process of known exponent, order~1 returns a displaced value and
orders~2 and~3 both recover the truth \impldetails{impl:global}. Everything below is
computed inside a retained segment, never across the gap that ends one
(Sec.~\ref{sec:uniform}), and never on displacement from take-off.

\paragraph{The fitted range.}
\SIrange{\StatVarParaFitMinS}{\StatVarParaFitMaxS}{\second}, \StatVarParaFitDecades\
decades, declared in advance for both disciplines and all three orders, with both ends set
by the system rather than the curve. Below, the Savitzky--Golay window has a floor of
\num{\PreprocSavgolMinWindow} samples (Sec.~\ref{sec:savgol}), and
\SI{\StatVarParaFitMinS}{\second} sits above that window for the slowest logger retained.
Above, the declared task takes over: the CFD scoring rule rewards a closed course, and a
closed course comes home at a time set by the task rather than the air. The fit is ordinary
least squares on $\log V_p$ against $\log\Delta$, applied to the mean of the per-flight
curves rather than to per-flight exponents averaged afterwards.

\begin{table}[htbp]
\centering\small
\ifrevmode\color{revblue}\fi
\renewcommand{\arraystretch}{1.2}
\caption{\rev{The order scan over
\SIrange{\StatVarParaFitMinS}{\StatVarParaFitMaxS}{\second}. Order~1 is the plain increment
and still carries each flight's course; orders~2 and~3 annihilate a constant velocity and a
constant acceleration, and agree with each other, which is what says that nothing polynomial
is left. The quoted interval combines a day-and-site bootstrap with the movement of the
exponent when the fitted range is halved; the two are given separately because they are not
of the same size, and the section that follows takes them apart.}}
\label{tab:orderscan}
\begin{tabular}{@{}l c c c c@{}}
\toprule
& \textbf{order $p$} & \boldmath$\alpha_p$ & \textbf{sampling} & \textbf{range} \\
\midrule
\multirow{3}{*}{paragliders}
 & 1 & \StatVarParaAlphaOrderOne\ $\pm$ \StatVarParaAlphaOrderOneErr
   & \StatVarParaAlphaOrderOneErrSampling & \StatVarParaAlphaOrderOneErrRange \\
 & 2 & \StatVarParaAlphaOrderTwo\ $\pm$ \StatVarParaAlphaOrderTwoErr
   & \StatVarParaAlphaOrderTwoErrSampling & \StatVarParaAlphaOrderTwoErrRange \\
 & 3 & \StatVarParaAlphaOrderThree\ $\pm$ \StatVarParaAlphaOrderThreeErr
   & \StatVarParaAlphaOrderThreeErrSampling & \StatVarParaAlphaOrderThreeErrRange \\
\addlinespace
\multirow{3}{*}{hang gliders}
 & 1 & \StatVarHangAlphaOrderOne\ $\pm$ \StatVarHangAlphaOrderOneErr
   & \StatVarHangAlphaOrderOneErrSampling & \StatVarHangAlphaOrderOneErrRange \\
 & 2 & \StatVarHangAlphaOrderTwo\ $\pm$ \StatVarHangAlphaOrderTwoErr
   & \StatVarHangAlphaOrderTwoErrSampling & \StatVarHangAlphaOrderTwoErrRange \\
 & 3 & \StatVarHangAlphaOrderThree\ $\pm$ \StatVarHangAlphaOrderThreeErr
   & \StatVarHangAlphaOrderThreeErrSampling & \StatVarHangAlphaOrderThreeErrRange \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The order scan.}
Table~\ref{tab:orderscan} gives the scan and Figure~\ref{fig:transport} the curves behind
it. At second order the archive returns
$\alpha_2=\StatVarParaAlphaOrderTwo\pm\StatVarParaAlphaOrderTwoErr$ for paragliders and
$\StatVarHangAlphaOrderTwo\pm\StatVarHangAlphaOrderTwoErr$ for hang gliders, over
\SIrange{\StatVarParaFitMinS}{\StatVarParaFitMaxS}{\second} in both --- agreement that was
not guaranteed, since the two disciplines differ in speed, glide ratio and where they fly.
The arch that dominates the ensemble MSD near \SI{\StatAuditParaEaSlopeMaxAt}{\second} is
three times smaller in $V_2$ and of the opposite sign: a dip rather than an arch, which is
what removing the common origin and the flown course was supposed to achieve
(Figure~\ref{fig:transport}).

\paragraph{Removing the course raises the exponent.}
$\alpha_1$ and $\alpha_2$ differ by $\StatVarParaDriftShare$ for paragliders and
$\StatVarHangDriftShare$ for hang gliders, and the sign is the opposite of what a residual
drift would leave: a drift normally inflates a displacement statistic, but on this archive
the declared course \emph{suppresses} it, since \SI{\StatVarParaClosedPct}{\percent} of
retained paraglider flights and \SI{\StatVarHangClosedPct}{\percent} of hang-glider flights
fly a triangle that returns towards its start, saturating the plain increment at a
task-set lag which the second-order variation does not see at all. Fitting closed and open
tasks separately confirms it directly: the gap between them (open minus closed) is
$\StatVarParaTaskGapOrderOne$ at order~1 for paragliders and $\StatVarHangTaskGapOrderOne$
for hang gliders --- closed tasks reading lower --- and it reverses to
$\StatVarParaTaskGapOrderTwo$ and $\StatVarHangTaskGapOrderTwo$ at order~2, with the order-1
gap growing substantially past the fitted range and the order-2 gap not moving. An exponent
read with no trend removed is therefore expected to sit below one measured with the course
filtered out, by the difference between the two orders.

\paragraph{Orders two and three agree, and order one checks against the time average.}
$\alpha_2$ and $\alpha_3$ agree to \StatVarParaOrderAgreement\ for paragliders and
\StatVarHangOrderAgreement\ for hang gliders, well inside either interval: nothing
polynomial survives the second difference for the third to remove. And $\alpha_1$, which
\emph{is} $\overline{\delta^2(\Delta)}$ under another name, computed by an independent
implementation over a different range, agrees with $\alpha_{\mathrm{TA}}$ of
Sec.~\ref{sec:obs-global} more closely than either moves when its own range is halved ---
$\StatVarParaAlphaOrderOne$ against $\StatMsdTaParaAlpha$ for paragliders and
\StatVarHangAlphaOrderOne\ against \StatMsdTaHangAlpha\ for hang gliders.

\paragraph{What an exponent near two does not mean.}
$\alpha_2=\StatVarParaAlphaOrderTwo$ is $2H$ with $H=\StatVarParaHurstOrderTwo$, at the upper
boundary of the self-similar family and marginally past it, and $\alpha_1$ and $\alpha_2$ do
not share a ceiling: reading the second against the first's bound is the only way this looks
like a violation. A plain increment cannot report above the ballistic value on any process
with stationary increments: the displacement over $n$ steps is a sum of $n$ increments,
Minkowski's inequality bounds its norm by the sum of theirs, and $V_1(n\Delta)\le n^2V_1(\Delta)$
follows, so $\alpha_1\le2$ whatever the motion does --- a bound the archive respects with room
to spare, at $\StatVarParaAlphaOrderOne$ and \StatVarHangAlphaOrderOne. The second-order
variation answers to a different bound because it is a different quantity: on a
differentiable trajectory $A_2\,\mathbf{r}\simeq\ddot{\mathbf{r}}\,\Delta^2$, so
$V_2\sim\Delta^{4}$, and its ceiling is therefore $4$, twice the ballistic value, reached only
by a trajectory smooth enough that its curvature does not wash out as $\Delta\to0$. A straight
line sits at the opposite extreme, where the second difference vanishes identically, since it
has no curvature to detect; a persistent random walk, calibrated the same way, returns a value
between the two, closer to $4$ the longer it stays correlated. $\StatVarParaAlphaOrderTwo$ and
$\StatVarHangAlphaOrderTwo$ sit just past $2H=2$, far below that ceiling of $4$: read against
the calibration that actually bounds this estimator, not against the ballistic value, which
bounds a different one, they say the trajectory stays correlated and rough --- persistent, not
straight --- at every lag of the range, not that it moves faster than a bound it was never
subject to. A Hurst exponent above unity has no meaning for an exactly self-similar process,
so the figure is quoted as an effective exponent over a declared window; how far the
archive is from exact self-similarity is measured in Sec.~\ref{sec:transport-propagator}, on
the distribution of the increments rather than its second moment.
\end{revblock}

\begin{figure}[htbp]
  \centering
  \IfFileExists{generated/transport.pdf}{\includegraphics[width=\linewidth]{generated/transport}}{%
    \fbox{\parbox{0.8\linewidth}{\centering\small Figure generated locally by
    \texttt{scripts/reporting/generate\_transport\_figure.py}.}}}
  \caption{\rev{The filtered variation, on which the exponent is read. (a)~$V_2(\Delta)$
  against lag, log--log, fitted range shaded. (b)~Local
  $H=\tfrac12\,\mathrm{d}\log V_p/\mathrm{d}\log\Delta$ at the three filter orders: $p=1$
  runs below $p=2,3$, the flown course read as a lower exponent. (c)~Local slope at $p=1$
  split by declared task; closed and open courses part company past \SI{3000}{\second}.
  (d)~Local slope of $V_2$, the quantity the fit averages.}}
  \label{fig:transport}
\end{figure}

\section{The uncertainty on the exponent}
\label{sec:transport-measure}
\begin{revblock}
The archive holds \num{\StatMsdParaFlights} paraglider and \num{\StatMsdHangFlights}
hang-glider flights, and \num{\StatVarParaClusters} and \num{\StatVarHangClusters} occasions
on which one site was flown on one day. An interval built on the first pair of counts is
not an interval built on the second, so which of them carries the independent measurements
has to be settled before either is quoted.

\paragraph{The resampling unit.}
The quantity clustered is the logarithm of each flight's own first-order variation at the
lag nearest the geometric centre of the fitted window, the per-flight input every fit in
this chapter averages over. Its
\flow{intraclass correlation at a level $\ell$ is
\begin{equation}
  \mathrm{ICC}_\ell = \frac{\sigma^2_{\text{between},\,\ell}}
    {\sigma^2_{\text{between},\,\ell}+\sigma^2_{\text{within},\,\ell}},
  \label{eq:icc}
\end{equation}
the share of the total variance sitting \emph{between} clusters at that level rather than
within them;} near zero the clustering is irrelevant, near one a cluster is effectively a
single measurement. The ladder (Table~\ref{tab:icc}) is zero at the flight by construction
and largest at the day and site in both disciplines --- the air over one site on one day
being what two flights actually have in common --- so every bootstrap in this chapter
resamples days and sites, of which the archive holds \num{\StatVarParaClusters} and
\num{\StatVarHangClusters}. The correction this buys is small and in the expected
direction: resampling flights returns \StatVarParaAlphaOrderOneErrNaive\ on the paraglider
$\alpha_1$ against \StatVarParaAlphaOrderOneErrSampling\ from the clustered resample, both
sitting below the last digit of the exponent they qualify.

\begin{table}[htbp]
\centering\small
\ifrevmode\color{revblue}\fi
\renewcommand{\arraystretch}{1.2}
\caption{\rev{Intraclass correlation of $\log V_1$ by clustering level. Zero at the flight
is the assumption a per-flight bootstrap makes, not a measurement; largest at the day and
site, which sets the resampling unit for every bootstrap in this chapter.}}
\label{tab:icc}
\begin{tabular}{@{}l c c c c@{}}
\toprule
& \textbf{flight} & \textbf{day} & \textbf{pilot} & \textbf{day \& site} \\
\midrule
paragliders & \StatVarParaIccFlight & \StatVarParaIccDay & \StatVarParaIccPilot & \StatVarParaIccDaySite \\
hang gliders & \StatVarHangIccFlight & \StatVarHangIccDay & \StatVarHangIccPilot & \StatVarHangIccDaySite \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The sampling term and the range term.}
The two are reported separately. On $\alpha_2$ the day-and-site bootstrap returns
\StatVarParaAlphaOrderTwoErrSampling\ for paragliders and
\StatVarHangAlphaOrderTwoErrSampling\ for hang gliders; refitting each half of the fitted
window and halving the difference between the two exponents returns
\StatVarParaAlphaOrderTwoErrRange\ and \StatVarHangAlphaOrderTwoErrRange. The sampling term
is negligible against the range term in both disciplines, and the quoted
$\pm\StatVarParaAlphaOrderTwoErr$ and $\pm\StatVarHangAlphaOrderTwoErr$ are the two combined
in quadrature --- the range term with the sampling term invisible inside it. The same
construction gives \StatVarParaAlphaOrderOneErrRange\ at order one and
\StatVarParaAlphaOrderThreeErrRange\ at order three, growing with the order of the filter as
a higher difference of a noisier curve should: the second decimal of the exponent is not
determined, and a number carrying the bootstrap alone would claim four significant figures
on a curve that does not have two.

A third figure, the standard error least squares returns on the fitted slope, measures
something else: how well a \emph{straight line} describes the fitted points, treating each
residual as an independent draw from a common noise. The residuals of these curves are
neither independent (every lag averages the same flights) nor noise (the local slope drifts
across the fitted decades), so that figure is dominated by the curve's departure from a
power law rather than by sampling, and is not an interval on the exponent
\impldetails{impl:global}.

\paragraph{What sets the two ends of the window.}
Below, the smoothing floor of \num{\PreprocSavgolMinWindow} samples (Sec.~\ref{sec:savgol})
sets \SI{\StatVarParaFitMinS}{\second} above the scale over which every retained cadence has
been made smooth. Above, the declared task governs: closed and open courses separate in
local slope by \num{\StatVarParaTaskSlopeGapLow} at
\SI{\StatVarParaTaskSlopeGapLowS}{\second} and by \num{\StatVarParaTaskSlopeGapHigh} at
\SI{\StatVarParaTaskSlopeGapHighS}{\second} for paragliders, from
\num{\StatVarHangTaskSlopeGapLow} to \num{\StatVarHangTaskSlopeGapHigh} for hang gliders
(Figure~\ref{fig:transport}), growing smoothly with no lag at which the measurement becomes
clean; the ceiling \SI{\StatVarParaFitMaxS}{\second} is set where that separation is still a
small fraction of what it reaches, leaving a window \StatVarParaFitDecades\ decades wide.

\paragraph{Four cuts of the ensemble.}
Table~\ref{tab:strata-alpha} asks whether $\alpha_2$ belongs to the motion or to who is in
the ensemble, on the same row-selection pass used throughout this chapter
\impldetails{impl:global}.

\begin{table}[htbp]
\centering\small
\ifrevmode\color{revblue}\fi
\renewcommand{\arraystretch}{1.2}
\caption{\rev{What $\alpha_2$ does when the paraglider ensemble is cut four ways, over the same
range. The comparison that matters is with Sec.~\ref{sec:prelim}, where the same wing classes
departed from the pooled ensemble MSD by
\SI{\StatPrelimParaClassTypicalDeparturePct}{\percent} and the orographic groups by
\SI{\StatPrelimParaGroupTypicalDeparturePct}{\percent}. The declared task is entered as the
open-minus-closed difference rather than as a spread, and it is the largest of the four.}}
\label{tab:strata-alpha}
\begin{tabular}{@{}l l c c@{}}
\toprule
\textbf{cut} & \textbf{strata} & \boldmath$\alpha_2$ \textbf{range} & \textbf{spread} \\
\midrule
native cadence & \StatVarParaCadenceStrata\ classes
  & \StatVarParaCadenceAlphaMin--\StatVarParaCadenceAlphaMax
  & \StatVarParaCadenceSpreadAlpha \\
wing class & \StatVarParaClassStrata\ classes
  & \StatVarParaClassAlphaMin--\StatVarParaClassAlphaMax
  & \StatVarParaClassSpreadAlpha \\
season & \StatVarParaSeasonStrata\ seasons
  & \StatVarParaSeasonAlphaMin--\StatVarParaSeasonAlphaMax
  & \StatVarParaSeasonSpreadAlpha \\
declared task & closed, open & --- & $\StatVarParaTaskGapOrderTwo$ \\
\bottomrule
\end{tabular}
\end{table}

The cadence cut is a check on the pipeline: across \num{\StatVarParaCadenceStrata} classes
spanning the whole range of smoothing scales in the archive, the exponent spreads by only
\StatVarParaCadenceSpreadAlpha, so whatever the estimator is reading, it is not the filter.
Wing class and season are the heterogeneity confound measured on the ensemble MSD in
Sec.~\ref{sec:prelim} (departures of \SI{\StatPrelimParaClassTypicalDeparturePct}{\percent}
and \SI{\StatPrelimParaGroupTypicalDeparturePct}{\percent}): here the exponent spreads by
only \StatVarParaClassSpreadAlpha\ across \num{\StatVarParaClassStrata} wing classes and
\StatVarParaSeasonSpreadAlpha\ across \num{\StatVarParaSeasonStrata} seasons, since a mixture
of populations with different speeds and courses is a mixture of trends, and a trend is what
the second difference removes. The heterogeneity that makes the ensemble estimator unusable
does not reach this one. The declared task is the largest of the four, at
$\StatVarParaTaskGapOrderTwo$ for paragliders and $\StatVarHangTaskGapOrderTwo$ for hang
gliders --- the one cut that divides flights by something other than who was flying --- and
no entry in the table exceeds the interval the exponent already carries.

\paragraph{The task stratum and the range term are one effect, not two.}
The task gap at order two, $\StatVarParaTaskGapOrderTwo$, and the range dependence,
\StatVarParaAlphaOrderTwoErrRange, are the same size and plausibly the same effect: both
measure how much of the fitted exponent is fixed by the long end of the window. The quoted
$\pm\StatVarParaAlphaOrderTwoErr$ already carries this systematic through its range half, so
entering the task stratum as a further term would count the scoring rule twice. For hang
gliders the two figures, $\StatVarHangTaskGapOrderTwo$ and \StatVarHangAlphaOrderTwoErrRange,
are of the same order but not the same size, so the identification is weaker there and the
interval is left as the range term alone.

\paragraph{How many exponents the range supports.}
A piecewise-linear fit to $\log V_2$ against $\log\Delta$, with $k$ breaks scored by a
Bayesian information criterion of the standard form,
\begin{equation}
  \mathrm{BIC}(k) = n_{\mathrm{eff}}\log\!\big(\mathrm{RSS}_k/n_{\mathrm{eff}}\big)
    + p_k\log n_{\mathrm{eff}},
  \label{eq:bic-breaks}
\end{equation}
$\mathrm{RSS}_k$ the fit's residual sum of squares and $p_k$ the number of free parameters
at $k$ breaks, but with $n_{\mathrm{eff}}=\StatVarParaDof$ the same effective degrees of
freedom used throughout this chapter substituted for the \num{\StatVarParaFitLags} nominal
lags, so a curve whose residuals are correlated cannot buy a break with points it does not
independently have. This selects \StatVarParaBreaks\ break for paragliders at
\SI{\StatVarParaBreakOneS}{\second} ---
separating \StatVarParaSlopeLow\ from \StatVarParaSlopeHigh, bracketing the single-exponent
value by about the size of the range dependence --- and \StatVarHangBreaks\ for hang
gliders. Run on surrogates that are a single power law plus the measured sampling noise, the
same procedure invents a break \SI{\StatVarParaFalsePositivePct}{\percent} and
\SI{\StatVarHangFalsePositivePct}{\percent} of the time \impldetails{impl:global}, too often
for either count to be trusted: the correlation across lags puts \num{\StatVarParaFitLags}
lags at no more than \StatVarParaDof\ effective points, a budget for one exponent and not for
a count of regimes.
\end{revblock}

\section{The exponent from the increment quantiles}
\label{sec:transport-propagator}
\begin{revblock}
Every exponent quoted so far is a second moment of a displacement, and this archive has
already shown what a second moment can measure instead of the motion: averaged about a
common origin, it returned the geometry of the launch site. What is wanted is a route to
the exponent that uses neither a moment nor a common origin.

\paragraph{First-passage times, tried and withdrawn.}
The first-passage time $T(L)$, the first time a flight is a distance $L$ from take-off,
gives $\langle T(L)\rangle\sim L^{1/H}$ for a self-similar process~\cite{redner2001}, so its
slope in the space domain was tried as a cross-check on $\alpha_2$. Read naively it exceeds
the ballistic value because of right censoring (only the slow minority of flights survive to
the largest radii); a Kaplan--Meier median removes the bias and lands inside the paraglider
interval it was meant to test, but moves across sub-ranges of radius by several times that
interval \impldetails{impl:global}. $T(L)$ is measured from take-off, so it carries the same
launch-geometry origin into the space domain --- the crossover of Figure~\ref{fig:msd} read
along the space axis instead of the time axis --- and the check is withdrawn as not
independent of it.

\paragraph{The propagator of the increments.}
Write $\Delta x(t_0,\Delta)=x(t_0+\Delta)-x(t_0)$ for the increment of one horizontal
component over a lag $\Delta$, inside a retained segment and never across the gap that ends
one (Sec.~\ref{sec:uniform}). Pooled over starting times and flights, its distribution is
the propagator of the increments,
\begin{equation}
  P(x,\Delta)\,\mathrm{d}x=\Pr\!\bigl(\Delta x\in[x,\,x+\mathrm{d}x]\bigr),
\end{equation}
and for a self-similar process with stationary increments it takes the scaling form
\begin{equation}
  P(x,\Delta)=\Delta^{-H}\,F\!\left(\frac{x}{\Delta^{H}}\right),
  \label{eq:increment-scaling}
\end{equation}
with $F$ a fixed density independent of the lag, defined on one component since pooling the
two would presuppose the isotropy Sec.~\ref{sec:prelim} rejects. Every quantile of
$|\Delta x|$ then grows as $\Delta^{H}$: writing $q_p(\Delta)$ for the $p$th quantile,
$\log q_p$ against $\log\Delta$ is a straight line of slope $H$ at every $p$, fixed by the
middle of the distribution where every flight contributes and a heavy tail moves it not at
all. Neither of the two defects of the earlier estimates enters here: no moment for a few
large increments to set, and no absolute position for the launch site to bias. The four
levels read are the lower quartile, the median, the upper quartile and the ninetieth
percentile, from one streaming pass keeping a histogram of $|\Delta x|$ per lag, per
component and per native cadence \impldetails{impl:global}.

\paragraph{What it returns.}
$H=\StatPropParaHurst$ for paragliders and $\StatPropHangHurst$ for hang gliders, over
\SIrange{\StatPropParaFitMinS}{\StatPropParaFitMaxS}{\second}, from
\num{\StatPropParaIncrements} and \num{\StatPropHangIncrements} increments in
\num{\StatPropParaCadences} and \num{\StatPropHangCadences} native cadence classes ---
doubled into the units the filtered variation is quoted in, $\alpha=\StatPropParaAlpha$ and
$\StatPropHangAlpha$. Across the paraglider cadence classes, spanning a factor of ten in
smoothing scale, the estimate moves between \StatPropParaHurstMin\ and
\StatPropParaHurstMax, so the smoothing scale does not set it.

\begin{table}[htbp]
\centering\small
\ifrevmode\color{revblue}\fi
\renewcommand{\arraystretch}{1.2}
\caption{\rev{Three routes to $H$, on the same ensemble and the same window,
\SIrange{\StatPropParaFitMinS}{\StatPropParaFitMaxS}{\second}. The first and the third are
second moments and differ in the frame they are taken in: the plain increment retains each
flight's course, the second-order variation annihilates it. The second is not a moment. The
first is the quantity a published exponent for human soaring flight measures; the third is
the route the chapter's $\alpha_2$ comes from, at $\alpha_2=2H$.}}
\label{tab:threeroutes}
\begin{tabular}{@{}l l c c@{}}
\toprule
\textbf{route} & \textbf{what it uses} & \boldmath$H$ \textbf{paragliders} & \boldmath$H$ \textbf{hang gliders} \\
\midrule
plain increment $V_1$ & second moment, course retained
  & \StatVarParaHurstOrderOne & \StatVarHangHurstOrderOne \\
increment quantiles & four order statistics
  & \StatPropParaHurst & \StatPropHangHurst \\
second-order variation $V_2$ & second moment, course filtered
  & \StatVarParaHurstOrderTwo & \StatVarHangHurstOrderTwo \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Three routes, and what they bracket.}
Table~\ref{tab:threeroutes} sets the quantile exponent beside the two second moments. They
bracket it: $H$ lies between \StatVarParaHurstOrderOne\ and \StatVarParaHurstOrderTwo\ for
paragliders and between \StatVarHangHurstOrderOne\ and \StatVarHangHurstOrderTwo\ for hang
gliders, nearer the plain increment. The gap between the two moments is the flown course,
measured by the order scan and read here from the other side, and it fixes which row a
published value is compared against: Vilpellet et al.~\cite{vilpellet2026} report a global
$H$ for human soaring flight from a displacement statistic with no trend removed --- the
plain increment of the first row, not the second-order variation. The paraglider value in
that row rounds to the reported one, on an independent archive and at the precision it is
quoted to; the second-order variation sits above it, with the sign of the gap the one the
order scan predicts, since the archive's closed tasks suppress displacement rather than
inflate it. The bracket is the result rather than any of its decimals: two second moments
taken in different frames and one statistic that is no moment at all, none of them measured
from take-off.

\paragraph{What the quantiles cost the scaling form.}
Equation~\eqref{eq:increment-scaling} claims the four quantiles share a slope. They do not:
fitted separately, $H$ falls from \StatPropParaHurstBulk\ at the lower quartile and the
median to \StatPropParaHurstFlank\ at the upper quartile and the ninetieth percentile for
paragliders, and from \StatPropHangHurstBulk\ to \StatPropHangHurstFlank\ for hang gliders,
in the same direction in both disciplines, on both components and at every cadence
(Figure~\ref{fig:propagator}b). The spread across the four quantiles is
\StatPropParaQuantileSpread\ and \StatPropHangQuantileSpread, against \StatPropCalibSpread\
on an exactly self-similar process fitted the same way with less data
\impldetails{impl:global}; rescaling the histograms by $\Delta^{H}$ leaves
\StatPropParaCollapseDex\ and \StatPropHangCollapseDex\ dex of scatter through the bulk,
with visible separation at the flanks (Figure~\ref{fig:propagator}c). The scaling form is
therefore only approximate: the centre of the increment distribution spreads faster than
its flanks, a distribution changing shape with scale rather than a single self-similar one.
That qualifies the exponent rather than refuting it --- the distance from bulk to flank is
small beside the distance of either from the Brownian value, and no reading of these curves
puts the transport near it --- but it is a second reason, independent of the range
dependence of $\alpha_2$, why a single exponent quoted to three figures would claim more
than this archive supports.

\paragraph{The anisotropy reaches the exponent.}
The two horizontal components differ in exponent as well as amplitude:
\StatPropParaHurstEast\ east against \StatPropParaHurstNorth\ north for paragliders, and
\StatPropHangHurstEast\ against \StatPropHangHurstNorth\ for hang gliders, the wider
separation of the two. Pooling the components would average that away, which is why they
are kept apart here and in Sec.~\ref{sec:prelim}.
\end{revblock}

\begin{figure}[htbp]
  \centering
  \IfFileExists{generated/propagator.pdf}{\includegraphics[width=\linewidth]{generated/propagator}}{%
    \fbox{\parbox{0.8\linewidth}{\centering\small Figure generated locally by
    \texttt{scripts/reporting/generate\_propagator\_figure.py}.}}}
  \caption{\rev{The exponent read from the distribution of the increments rather than a
  moment of it. (a)~25th/50th/75th/90th percentiles of $|\Delta x|$ against lag, log--log; a
  self-similar process gives four parallel lines. (b)~Exponent fitted at each percentile
  separately, against the dashed self-similar value; every curve falls from bulk to flank.
  (c)~Histograms rescaled by $\Delta^{H}$: they coincide through the bulk and part company
  at the flanks.}}
  \label{fig:propagator}
\end{figure}

\section{The archive is not a L\'evy walk}
\label{sec:transport-shape}
\begin{revblock}
An exponent fixes one moment of one distribution, and two processes can agree on it and be
different physics. A correlated Gaussian motion and a L\'evy walk both give
$\langle|\Delta\mathbf{r}|^2\rangle\sim\Delta^{2H}$; what separates them is the rest of the
moment spectrum. It is measured on the same unsegmented trajectories, with no estimator in
common with the exponent itself, and it refuses the L\'evy walk.

\paragraph{The moment spectrum.}
Write
\begin{equation}
  \big\langle|\Delta\mathbf{r}(\Delta)|^{q}\big\rangle\sim\Delta^{\,q\nu(q)}
  \label{eq:moment-spectrum}
\end{equation}
and read $q\nu(q)$ as a function of $q$. A self-similar monofractal process gives a straight
line through the origin with $\nu(q)$ constant; a L\'evy walk --- flight durations drawn
from a power-law tail, each traversed at finite speed --- gives a bilinear one, with a knee
at its tail index and a ballistic right-hand branch of slope one. Over
\SIrange{\StatShapeParaFitMinS}{\StatShapeParaFitMaxS}{\second}, on \num{\StatShapeParaLags}
lags and $q$ from \StatShapeParaQMin\ to \StatShapeParaQMax, the spectrum is straight
(Figure~\ref{fig:shape}a): $\nu(q)$ varies by \StatShapeParaNuSpread\ for paragliders and
\StatShapeHangNuSpread\ for hang gliders about a single fitted $\nu=\StatShapeParaLinearSlope$
and $\StatShapeHangLinearSlope$, and the rms departure of $q\nu(q)$ from a line through the
origin, \StatShapeParaLinearDeparture\ and \StatShapeHangLinearDeparture, sits an order of
magnitude below what a L\'evy walk's knee leaves on this grid and range of $q$
\impldetails{impl:global}. The moments are estimable rather than merely computed: the share
of each moment carried by the largest hundredth of its increments reaches
\SI{\StatShapeParaTailMaxPct}{\percent} at worst for paragliders and
\SI{\StatShapeHangTailMaxPct}{\percent} for hang gliders (Figure~\ref{fig:shape}b), well
short of the half that would make a moment a statement about a few flights rather than an
estimate. On the whole trajectory, unsegmented, the archive does not support the L\'evy walk
this thesis was framed around, and what the spectrum leaves standing is a monofractal
process: a single exponent governing every moment over the range read.

The previous section's quantile fit found that $H$ falls from bulk to flank rather than
holding a single slope, which looks in tension with a straight spectrum. The two are
compatible: a moment of order $q$ integrates the whole increment distribution, so a local
exponent drifting between centre and flanks enters $\nu(q)$ weighted by where that moment
takes its mass, and the drift partly cancels, while a quantile fit reads the collapse at a
fixed probability level and averages nothing. The quantile spread
(\StatPropParaQuantileSpread, \StatPropHangQuantileSpread) is accordingly the smaller of the
two figures against the spread of $\nu(q)$ (\StatShapeParaNuSpread, \StatShapeHangNuSpread),
though only the ordering is established, not a derived bound; the refusal of the L\'evy walk
is untouched either way, since the departure a knee would leave exceeds both.

What survives is a monofractal process, one exponent for every moment over the range read,
which fixes how the increment distribution scales and says nothing about its shape.
\end{revblock}

\begin{figure}[htbp]
  \centering
  \IfFileExists{generated/shape.pdf}{\includegraphics[width=\linewidth]{generated/shape}}{%
    \fbox{\parbox{0.8\linewidth}{\centering\small Figure generated locally by
    \texttt{scripts/reporting/generate\_shape\_figure.py}.}}}
  \caption{\rev{What the second moment cannot say. (a)~Moment spectrum $q\nu(q)$, straight
  line dashed: straight means one exponent for every moment. (b)~Share of each moment
  carried by the largest \SI{1}{\percent} of increments. (c)~Non-Gaussian parameter against
  lag, Gaussian and L\'evy-walk values marked. (d)~Velocity autocorrelation, log--log, fitted
  tail dashed.}}
  \label{fig:shape}
\end{figure}

\section{The propagator is not Gaussian}
\label{sec:transport-gaussian}
\begin{revblock}
A straight moment spectrum says that one exponent governs every moment of the increment
distribution. It fixes how that distribution \emph{scales} and says nothing about its
\emph{shape}, so calling the process a correlated Gaussian motion does not follow from it.
The fourth and second moments already accumulated settle the shape directly, through the
non-Gaussian parameter
\begin{equation}
  \alpha_2^{\mathrm{NG}}(\Delta)=
  \frac{\big\langle|\Delta\mathbf{r}(\Delta)|^{4}\big\rangle}
       {2\,\big\langle|\Delta\mathbf{r}(\Delta)|^{2}\big\rangle^{2}}-1 ,
  \label{eq:nongauss}
\end{equation}
evaluated on the increment over a lag $\Delta$ rather than the displacement from take-off,
normalised so it vanishes for a Gaussian propagator and is positive for heavier-than-Gaussian
tails. The superscript distinguishes it from the filtered-variation exponent $\alpha_2$, an
unrelated quantity that happens to share the symbol at filter order $p=2$.

Over \SIrange{\StatShapeParaNonGaussMinS}{\StatShapeParaNonGaussMaxS}{\second} the median
value is \StatShapeParaNonGaussMedian\ for paragliders and \StatShapeHangNonGaussMedian\ for
hang gliders. Neither is flat: both rise to an interior maximum and fall back ---
\StatShapeParaNonGaussPeak\ at \SI{\StatShapeParaNonGaussPeakS}{\second} for paragliders and
\StatShapeHangNonGaussPeak\ at \SI{\StatShapeHangNonGaussPeakS}{\second} for hang gliders,
monotone over seven or more consecutive lags on each side, a shape and not scatter. A maximum
at two to four hundred seconds is a scale the chapter has otherwise found none of: it is the
lag where the \emph{shape} of the increment distribution departs furthest from Gaussian,
smaller below it (increments are averages over a smoothing window and a few heading changes)
and above it (a central-limit Gaussianisation begins to act). Reported against processes of
known shape, both archives are non-Gaussian and neither is close to a L\'evy walk: on an
exact fractional Brownian motion the same estimator returns a small positive value at a
matched sample size, collapsing towards zero as the sample grows, while on a L\'evy walk it
returns a figure far above either archive's and does not move with the sample
\impldetails{impl:global}. The two archives do not overlap at any lag in the window, a
separation larger than the movement either curve shows across it, acting more strongly on
the smaller, faster discipline by the ratio of the two median ground speeds
(Sec.~\ref{sec:transport-kinematics}).

\paragraph{The pooled parameter is a width mixture, not a tail.}
The value is pooled by construction: the moments are accumulated over every flight before
any ratio is taken, so it is a property of the whole population of increments and not of
any flight's own propagator. Writing $S_f$ for a flight's own second moment and $a_f$ for
its own non-Gaussian parameter,
\begin{equation}
  1 + \alpha^{\mathrm{NG}}_{\text{pooled}}
    = \frac{\mathbb{E}_n\!\left[(1 + a_f)\,S_f^{2}\right]}{\mathbb{E}_n[S_f]^{2}} .
  \label{eq:pooled-nongauss}
\end{equation}
Setting every $a_f$ to zero --- every flight exactly Gaussian --- leaves
$\mathrm{CV}^{2}(S_f)$, the excess a population of Gaussian flights of differing amplitude
produces on its own; that null is \emph{above} the measurement at every one of
\num{\StatShapeParaNullLags} lags for paragliders and \num{\StatShapeHangNullLags} for hang
gliders. Subtracting it, the within-flight excess is \emph{negative} ---
\StatShapeParaWithinFlight\ and \StatShapeHangWithinFlight\ in the median --- so a flight's
own increment distribution is flatter than Gaussian, not heavy-tailed, which is what a wing
holding a characteristic airspeed should give. This changes what the number means rather
than what it is: the pooled propagator genuinely is not Gaussian, because a mixture of
Gaussians of differing width is not one, but the excess is the width mixture and not a tail,
and what has to be reproduced is the between-flight amplitude spread,
$\mathrm{CV}(S_f)=\StatShapeParaAmplitudeCv$ against $\StatShapeHangAmplitudeCv$ at
\SI{\StatShapeParaNullFloorS}{\second}. The refusal of the L\'evy walk is untouched, resting
on the moment spectrum, measured per lag and not pooled across flights of different
amplitude.

Beyond the fitted range $\alpha_2^{\mathrm{NG}}$ climbs, to \StatShapeParaNonGaussBeyond\ and
\StatShapeHangNonGaussBeyond\ at the longest lags of panel~(c): reported and not quoted,
since there the declared task governs the trajectory and the moment is carried by the fewest
flights. The process is monofractal and its propagator is not Gaussian: one exponent for
every moment fixes how the increment distribution scales, and leaves the shape it scales as
heavy as it is measured to be.
\end{revblock}

\section{The memory of the heading}
\label{sec:transport-memory}
\begin{revblock}
Writing $\delta\mathbf{v}$ for the horizontal velocity fluctuation about a record's own
mean, the velocity autocorrelation is
\begin{equation}
  C(\tau)=\frac{\bigl\langle\,\delta\mathbf{v}(t_0)\cdot\delta\mathbf{v}(t_0+\tau)\,\bigr\rangle_{t_0}}
               {\bigl\langle\,|\delta\mathbf{v}(t_0)|^{2}\,\bigr\rangle_{t_0}},
  \label{eq:vacf}
\end{equation}
normalised so that $C(0)=1$, with the flown course, which would otherwise hold every lag
up, removed before the average is taken. It is computed within a retained segment and never
across the gap that ends one (Sec.~\ref{sec:uniform}), by the Fourier identity rather than
by a double loop, and averaged across records at each lag of the grid the moments use
\impldetails{impl:global}.

\paragraph{Positive at every lag, and small at the first.}
$C$ is positive throughout Figure~\ref{fig:shape} and changes sign nowhere on the grid:
$C=\StatShapeParaVacfAtFloor$ for paragliders at the floor and $\StatShapeHangVacfAtFloor$
for hang gliders, decaying from there without a zero crossing. A single circling wing would
produce a sign change, its velocity pointing the other way half a turn later, but an ensemble
of them does not: radius and phase disperse across flights, so the average of many circles
cannot go negative even when every record in it turns. What survives an average is the
shape, not the sign --- a minimum at half a period and a maximum at the period --- and both
are there, below the first lag Figure~\ref{fig:shape} plots. Measured at native cadence on the
\SI{1}{\hertz} segments, $C$ falls to \StatCirclingParaTrough\ at
\SI{\StatCirclingParaTroughS}{\second} and recovers to \StatCirclingParaCrest\ at
\SI{\StatCirclingParaCrestS}{\second} for paragliders, and to \StatCirclingHangTrough\ at
\SI{\StatCirclingHangTroughS}{\second} and \StatCirclingHangCrest\ at
\SI{\StatCirclingHangCrestS}{\second} for hang gliders \impldetails{impl:global}, the minimum
sitting at half the maximum's lag in both disciplines --- the circle, not a coincidence of the
estimator, and nowhere near zero. So the unsegmented ensemble already carries the circling,
with a period of \SIrange{\StatCirclingHangCrestS}{\StatCirclingParaCrestS}{\second}, which a
phase decomposition in Chapter~\ref{ch:phases} can be held to rather than asked to invent.
And $C$ is already small at the bottom of the fitted grid, falling below $1/e$ by
\SI{\StatShapeParaVacfDecayS}{\second} for paragliders and
\SI{\StatShapeHangVacfDecayS}{\second} for hang gliders, within its first lags --- a property
of the pipeline as much as of the flying, since the grid starts at the smoothing scale of the
slowest logger retained (Sec.~\ref{sec:savgol}).

\paragraph{Integrability, not size, sustains the exponent.}
A memory spent by \SI{\StatVarParaFitMinS}{\second} looks incompatible with increments still
correlated to \SI{\StatVarParaFitMaxS}{\second}, but it is not the size of $C$ that sets the
growth law. For a stationary velocity the Green--Kubo relation
\begin{equation}
  \bigl\langle|\Delta\mathbf{r}(\Delta)|^{2}\bigr\rangle
  =2\!\int_{0}^{\Delta}\!(\Delta-\tau)\,C(\tau)\,\mathrm{d}\tau
  \label{eq:green-kubo}
\end{equation}
makes the growth law a statement about the convergence of an integral: a tail
$C(\tau)\sim\tau^{-\gamma}$ with $\gamma<1$ has no finite integral and gives
$\langle|\Delta\mathbf{r}|^{2}\rangle\sim\Delta^{\,2-\gamma}$ across the window however small
$C$ has already become, while $\gamma>1$ gives diffusive motion beyond the correlation time
whatever $C$ is worth at the floor; multiplying $C$ by a constant moves every value on it and
leaves $\gamma$ where it was. A single power law fitted to
\SIrange{\StatShapeParaVacfTailMinS}{\StatShapeParaVacfTailMaxS}{\second} gives
$\gamma=\StatShapeParaVacfTailGamma$ for paragliders and \StatShapeHangVacfTailGamma\ for
hang gliders, but that fit is itself an arch, the local slope running from
\StatShapeParaVacfGammaLocalMin\ to \StatShapeParaVacfGammaLocalMax\ across the same lags.
What the argument needs is not the fitted value: \emph{every} local slope is below unity, in
both disciplines and at all \num{\StatShapeParaVacfTailLags} lags, so non-integrability holds
pointwise rather than on a fitted average --- the stronger statement, and one that does not
depend on the tail being a power law at all \impldetails{impl:global}. Two features of the
estimate push $\gamma$ up rather than down (a mean velocity estimated from the same record
pulls the tail towards zero, and the first decade of the integral in
Eq.~\eqref{eq:green-kubo} lies below the grid), so the exponent $2-\gamma$ implied for the
displacement is a floor rather than an estimate --- \StatShapeParaVacfImpliedAlphaFloor\ and
\StatShapeHangVacfImpliedAlphaFloor\ --- and the plain increment,
$\alpha_1=\StatVarParaAlphaOrderOne$ and \StatVarHangAlphaOrderOne, clears both floors: a
consistency between two channels, not a second determination, that excludes a velocity
memory integrable over this window.

\paragraph{The turning angles.}
The persistence has a microscopic form: the angle between successive horizontal displacement
vectors, taken at a stride of \num{\StatKinParaAngleStrideSamples} samples so the smoothing
window shares no sample between the two vectors \impldetails{impl:global}. Its median is
\SI{\StatKinParaAngleMedianDeg}{\degree} for paragliders, over \num{\StatKinParaAngles}
angles, and \SI{\StatKinHangAngleMedianDeg}{\degree} for hang gliders, with
\SI{\StatKinParaAngleForwardPct}{\percent} and \SI{\StatKinHangAngleForwardPct}{\percent}
falling within $\pi/6$ of the previous heading; restricted to matched \SI{1}{\hertz} segments
the medians are \SI{\StatKinParaAngleMedianOneHzDeg}{\degree} and
\SI{\StatKinHangAngleMedianOneHzDeg}{\degree}, so the gap between disciplines survives
matching, losing about a quarter of its size. Memoryless reorientation, a right-angle median,
is nowhere near. A wing that changes heading by \SI{\StatKinParaAngleMedianDeg}{\degree} at a
time is a correlated walk, and a positive $C(\tau)$, a non-integrable tail, and increments
still correlated at \SI{\StatVarParaFitMaxS}{\second} are the ensemble consequences of the
same forward peak --- the warrant for the persistent random walk of Sec.~\ref{sec:modelling}.
The angles outside the forward cone are a minority of the same order as the climbing share of
Sec.~\ref{sec:prelim} (\SI{\StatKinParaClimbingPct}{\percent} for paragliders,
\SI{\StatKinHangClimbingPct}{\percent} for hang gliders): pooled, climbing and gliding give
one forward-peaked distribution and a $C(\tau)$ with no sign change, which is why the
circling is invisible to both statistics here and not absent from the flying. The
segmentation of Sec.~\ref{sec:segmentation} separates them, and the turning angles
conditioned on phase are where this result has to reappear.
\end{revblock}

\section{Speed and vertical velocity}
\label{sec:transport-kinematics}
\begin{revblock}
The horizontal speed and the vertical velocity are marginals of the smoothed kinematics of
Sec.~\ref{sec:savgol}, taken over every retained in-segment fix, accumulated in the same
streaming pass that builds the increment histograms of Sec.~\ref{sec:transport-propagator}
\impldetails{impl:global}. Table~\ref{tab:kinematics} gives both, each read against a
reference fixed outside the archive rather than against itself.

\begin{table}[htbp]
\centering\small
\ifrevmode\color{revblue}\fi
\renewcommand{\arraystretch}{1.2}
\caption{\rev{Marginal kinematics of the retained ensemble, against the fix-level bounds of
Sec.~\ref{sec:fixlevel}: \SI{\PreprocMaxHSpeedParaMps}{\metre\per\second} (paragliders) and
\SI{\PreprocMaxHSpeedHangMps}{\metre\per\second} (hang gliders) on the horizontal speed,
$\pm$\SI{\PreprocMaxVSpeedMps}{\metre\per\second} on the vertical velocity. A retained
population pressed against them would mean the bound had cut into the fast tail of the
flying rather than into instrument artefacts.}}
\label{tab:kinematics}
\begin{tabular}{@{}l c c@{}}
\toprule
& \textbf{paragliders} & \textbf{hang gliders} \\
\midrule
horizontal speed, median & \SI{\StatKinParaSpeedMedianMs}{\metre\per\second}
  & \SI{\StatKinHangSpeedMedianMs}{\metre\per\second} \\
horizontal speed, P90 & \SI{\StatKinParaSpeedNinetyMs}{\metre\per\second}
  & \SI{\StatKinHangSpeedNinetyMs}{\metre\per\second} \\
horizontal speed, P99 & \SI{\StatKinParaSpeedNinetyNineMs}{\metre\per\second}
  & \SI{\StatKinHangSpeedNinetyNineMs}{\metre\per\second} \\
vertical velocity, median & \SI{\StatKinParaVerticalMedianMs}{\metre\per\second}
  & \SI{\StatKinHangVerticalMedianMs}{\metre\per\second} \\
vertical velocity, 10--90\% band
  & \SIrange{\StatKinParaVerticalTenMs}{\StatKinParaVerticalNinetyMs}{\metre\per\second}
  & \SIrange{\StatKinHangVerticalTenMs}{\StatKinHangVerticalNinetyMs}{\metre\per\second} \\
climbing fraction & \SI{\StatKinParaClimbingPct}{\percent} & \SI{\StatKinHangClimbingPct}{\percent} \\
\bottomrule
\end{tabular}
\end{table}

The disciplines separate at every speed percentile, the hang-glider median lying above the
paraglider ninetieth percentile, and both ninety-ninth percentiles sit well inside their
fix-level bound. The vertical velocity is centred just below zero and stays inside its
bound at both ends. \flow{Writing $f_\uparrow$ for the climbing
fraction of a flight and $\langle\dot z\rangle_\uparrow$, $\langle\dot z\rangle_\downarrow$
for the mean climb and sink rates, a flight that ends near the altitude it began at has
$\int\dot z\,\mathrm{d}t\approx0$, i.e.
\begin{equation}
  f_\uparrow\,\langle\dot z\rangle_\uparrow
  + (1-f_\uparrow)\,\langle\dot z\rangle_\downarrow \approx 0
  \quad\Longrightarrow\quad
  \frac{f_\uparrow}{1-f_\uparrow} \approx
  \frac{|\langle\dot z\rangle_\downarrow|}{\langle\dot z\rangle_\uparrow},
  \label{eq:climb-balance}
\end{equation}
the climbing share required near a half whenever the two rates are comparable.} Over the
median flight of \SI{\StatPrelimParaMedianDurH}{\hour} and \SI{\StatPrelimHangMedianDurH}{\hour}
(Sec.~\ref{sec:prelim}) both measured shares fall just short of a half --- the same fact as
the negative median, since the wing spends more of its time sinking and its mean rate of
climb exceeds its mean rate of sink. The sign of the vertical velocity is the coarsest
climb--glide rule available, so these two shares are what a segmentation in
Chapter~\ref{ch:phases} must return as its threshold goes to zero.

\paragraph{The return probability.}
The return probability, the increment propagator at its centre
$P_0(\Delta)\equiv P(\mathbf{0},\Delta)$, decays as $\Delta^{-H}$ under the scaling form of
Sec.~\ref{sec:transport-propagator} and so is another reading of $H$, at a fifth probability
level costing a fit and no further traversal of the fix table; it is defined here and not
measured. First-passage times are not on this list either: they were measured and withdrawn
in Sec.~\ref{sec:transport-propagator}, since a time to reach a radius from take-off carries
the launch geometry into the space domain.
\end{revblock}

\section{What this establishes, and what the segmentation receives}
\label{sec:transport-verdict}
\begin{revblock}
Over \SIrange{\StatVarParaFitMinS}{\StatVarParaFitMaxS}{\second}, measured within a retained
segment by an estimator that annihilates a polynomial trend rather than estimating one,
cross-country soaring gives
$\alpha_2=\StatVarParaAlphaOrderTwo\pm\StatVarParaAlphaOrderTwoErr$ for paragliders and
$\StatVarHangAlphaOrderTwo\pm\StatVarHangAlphaOrderTwoErr$ for hang gliders, on
\num{\StatMsdParaFlights} and \num{\StatMsdHangFlights} flights. The wing classes that
depart from the pooled ensemble MSD by
\SI{\StatPrelimParaClassTypicalDeparturePct}{\percent} (Sec.~\ref{sec:prelim}) move this
exponent by \StatVarParaClassSpreadAlpha; native cadence, across a factor of ten in the
scale below which the trajectory has been smoothed, moves it by
\StatVarParaCadenceSpreadAlpha, season by \StatVarParaSeasonSpreadAlpha, and the declared
task by $\StatVarParaTaskGapOrderTwo$, the largest of the four. The two disciplines agree
within their uncertainties, which is not guaranteed: they differ in speed and in glide
ratio.

\paragraph{The class of process.}
Table~\ref{tab:fingerprint} collects every global number this chapter measures on the
unsegmented trajectory. Read together they fix a class rather than a list: over this window
the archive is a monofractal (one exponent for every moment, no Lévy knee), non-Gaussian
propagator (a between-flight width mixture, not a heavy tail within one record),
directionally persistent motion (a non-integrable velocity memory, forward-peaked turning)
that is never isotropic enough to summarise with one component instead of two --- neither
the Lévy walk this thesis was framed around nor a Gaussian one.

\begin{table}[htbp]
\centering\small
\ifrevmode\color{revblue}\fi
\renewcommand{\arraystretch}{1.25}
\caption{\rev{The process, in eight numbers: every global quantity this chapter measures on
the unsegmented trajectory, over \SIrange{\StatVarParaFitMinS}{\StatVarParaFitMaxS}{\second}.
Together they fix what the transport class is and rule out what it is not; each is measured
and qualified in the section of the same name earlier in this chapter.}}
\label{tab:fingerprint}
\begin{tabular}{@{}l c c l@{}}
\toprule
\textbf{Property} & \textbf{paragliders} & \textbf{hang gliders} & \textbf{reads as} \\
\midrule
transport exponent $\alpha_2=2H$
  & \StatVarParaAlphaOrderTwo\,$\pm$\,\StatVarParaAlphaOrderTwoErr
  & \StatVarHangAlphaOrderTwo\,$\pm$\,\StatVarHangAlphaOrderTwoErr
  & super-diffusive \\
$H$ bracket, 3 routes
  & \StatVarParaHurstOrderOne--\StatVarParaHurstOrderTwo
  & \StatVarHangHurstOrderOne--\StatVarHangHurstOrderTwo
  & scaling approximate \\
moment spectrum, $\nu(q)$ spread
  & \StatShapeParaNuSpread & \StatShapeHangNuSpread
  & monofractal \\
non-Gaussian, pooled median
  & \StatShapeParaNonGaussMedian & \StatShapeHangNonGaussMedian
  & non-Gaussian \\
non-Gaussian, within one flight
  & \StatShapeParaWithinFlight & \StatShapeHangWithinFlight
  & sub-Gaussian per flight \\
velocity memory, local $\gamma$
  & \StatShapeParaVacfGammaLocalMin--\StatShapeParaVacfGammaLocalMax
  & \StatShapeHangVacfGammaLocalMin--\StatShapeHangVacfGammaLocalMax
  & non-integrable \\
anisotropy, amplitude
  & \StatPrelimParaIsoRatioMedian & \StatPrelimHangIsoRatioMedian
  & never isotropic \\
anisotropy, exponent
  & \StatPropParaHurstEast\,/\,\StatPropParaHurstNorth
  & \StatPropHangHurstEast\,/\,\StatPropHangHurstNorth
  & direction-dependent \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The limits.}
The window is \StatVarParaFitDecades\ decades wide, and neither end is set by the size of
the archive. Below it the trajectory has been smoothed over a window whose floor is
\PreprocSavgolMinWindow\ samples and which therefore scales with the logger's cadence
(Sec.~\ref{sec:savgol}); nothing below that scale is read as motion. Above it the declared
task governs the displacement, the separation between closed and open courses in the local
slope growing from \num{\StatVarParaTaskSlopeGapLow} at
\SI{\StatVarParaTaskSlopeGapLowS}{\second} to \num{\StatVarParaTaskSlopeGapHigh} at
\SI{\StatVarParaTaskSlopeGapHighS}{\second}. Inside the window the scaling form is only
approximate: $H$ falls from \StatPropParaHurstBulk\ at the bulk of the increment
distribution to \StatPropParaHurstFlank\ at its flank, a spread across the quantiles of
\StatPropParaQuantileSpread\ and \StatPropHangQuantileSpread\ against \StatPropCalibSpread\
on an exactly self-similar process measured the same way. The exponent itself moves by
\StatVarParaAlphaOrderTwoErrRange\ when the fitted range is halved, against
\StatVarParaAlphaOrderTwoErrSampling\ from resampling at the day and site, and the
\num{\StatVarParaFitLags} lags of the fit carry no more than \StatVarParaDof\ effective
degrees of freedom, a budget for one exponent and not for a count of regimes. That the
transport is characterised by $\alpha=\StatVarParaAlphaOrderTwo$ is therefore a different,
stronger statement than the effective exponent quoted above, and the range dependence and
the quantile spread are each enough to withhold it. The window will not widen with more
data: a soaring day ends, so the upper cutoff follows from the system rather than the
method.

\paragraph{What was withdrawn.}
Four measurements are withdrawn. The ensemble MSD about take-off yields a timescale instead
of an exponent: displacement from the launch site crosses over where the population leaves
its launch area, at a median of \SI{\StatAuditParaDepartureMedianS}{\second} for paragliders
and \SI{\StatAuditHangDepartureMedianS}{\second} for hang gliders on the first crossing of
\SI{\StatAuditParaDepartureRadiusKm}{\kilo\metre}. The first-passage time was brought in as a
route to $H$ in the space domain and withdrawn when its exponent moved across sub-ranges of
radius by more than the uncertainty it was meant to check; what it bought is the sharpest
evidence that the ensemble curve's failure belongs to the common origin rather than the time
axis, since exchanging the time axis for a radius reproduces it \impldetails{impl:global}.
The sign-run count on the MSD residuals is kept as a description of how those residuals are
arranged and withdrawn as a test, every lag of that curve being an average over the same
flights; what carries the claim instead is the size of the residual against the standard
error on each lag. And the reading of a straight moment spectrum as a Gaussian propagator is
withdrawn, one exponent for every moment being a statement about how the distribution scales
and none about its shape; what it bought is the non-Gaussian parameter, computed from
moments the spectrum had already accumulated.

\paragraph{The hand-over.}
Chapter~\ref{ch:phases} inherits a class of transport and five constraints on how it may be
decomposed.

\begin{enumerate}[label=(\roman*)]
  \item Any statistic measured from take-off inherits the launch geometry, out to the
        departure times above, and the contamination is not confined to the observable that
        exposed it: any phase statistic binned by elapsed time since take-off carries it
        below those times. The remedy is the one used throughout this chapter, to measure
        within a segment and never about a common origin (Sec.~\ref{sec:uniform}).
  \item The estimator is additive over any partition of the sample: $V_2(\Delta)$ is a mean
        over the placements of a second difference, and labelling those placements by phase
        and recombining the per-phase means with the weights actually observed returns the
        pooled curve identically. That is a property of the estimator rather than of the
        transport, asserting no decomposition of an exponent into phase exponents, only that
        the arithmetic closes; a recombination that misses the pooled value is evidence
        about the segmentation and is to be read that way before it is read as physics.
  \item The two horizontal components are analysed separately and never pooled. They are
        incompatible in amplitude (Sec.~\ref{sec:prelim}) and differ in exponent:
        $H=\StatPropParaHurstEast$ east against \StatPropParaHurstNorth\ north for
        paragliders, and \StatPropHangHurstEast\ against \StatPropHangHurstNorth\ for hang
        gliders. Pooling presupposes an isotropy this ensemble does not have.
  \item The pooled non-Gaussian parameter is $\StatShapeParaNonGaussMedian$ for paragliders
        and $\StatShapeHangNonGaussMedian$ for hang gliders, and against a matched Gaussian
        null both sit \emph{below} it, by $\StatShapeParaWithinFlight$ and
        $\StatShapeHangWithinFlight$. What is handed on is therefore a between-flight
        amplitude spread and not an increment tail: $\mathrm{CV}(S_f) =
        \StatShapeParaAmplitudeCv$ against $\StatShapeHangAmplitudeCv$ at
        \SI{\StatShapeParaNullFloorS}{\second}. A phase decomposition has to produce that
        spread across records, and must not produce a heavy tail within one.
  \item The ergodicity question is handed on in two forms that need no power law: the drift
        of the exponent with the time already flown, obtained by running the same
        within-segment estimator on successive stretches of a record
        (Sec.~\ref{sec:segmentation}); and the ergodicity-breaking parameter of
        Appendix~\ref{app:ctrw}, which compares the distribution of the per-flight time
        average across flights rather than two fitted slopes. The usual comparison of
        $\alpha_{\mathrm{EA}}$ with $\alpha_{\mathrm{TA}}$ requires both curves to be power
        laws, and this archive supplies one of the two.
\end{enumerate}

The class of transport is fixed over one window; which flight behaviour produces it is not.
Every number above mixes climbing, gliding and searching into one, and the persistent random
walk of Sec.~\ref{sec:modelling} takes its warrant from the memory measured here rather than
from any single one of them. Chapter~\ref{ch:phases} separates the phases, and is answerable
to the five constraints.
\end{revblock}
