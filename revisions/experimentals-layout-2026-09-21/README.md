# Experimentals endmatter relocation — 2026-09-21

Moved the former Section 3.2.4 to the new Experimentals endmatter after both
appendices, as Section E.1. The factorial prose, numerical results, figures,
tables and citations are retained. Only the heading level and introductory
location references change. Section 3.2 retains a forward cross-reference.
Equations, figures and tables now have the E prefix; all original labels persist.
The measurement pipeline and published data are unchanged.

The reader-facing PDF is replaced once, after an isolated LaTeX build and
visual review. Source snapshots before relocation are in `before/`.

Validation: 92 PDF pages; Experimentals is on printed pages 84–88 (PDF pages
88–92). All 11 factorial labels are retained once, and the method/results body
is unchanged. The final log has no undefined references, overfull boxes or
duplicate destinations. Ten affected/transition pages were visually inspected.
The source snapshot matched at publication, and the reader PDF was replaced once.
