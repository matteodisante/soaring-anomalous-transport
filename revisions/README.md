# Scientific review records

The current revision is closed in
[the completion record](manuscript-review-2026-09-11/completion.json), with the
[request checklist](request-checklist-2026-09-11.md) and
[interpretation of fresh results](fresh-results-review-2026-09-11.md).
Cleaning 2.2.0 and all 38 rebuild stages completed; the final manuscript and the
corrected dataset report were reconciled against the mounted SSD archive.

The dated chapter audits preserve findings and validation at the time they were
written. Their intermediate test counts and statements that regeneration was still
pending are historical; the completion record gives the final status. Likewise,
signed `proof.json` files preserve the state at each review step. Their hashes must
not be changed to rewrite history. The later reconciliation record identifies the
proofs whose initially pending SSD checks have now been completed.

The evidence distinguishes four operations:

- cleaning and numerical analysis, with original run manifests;
- documentation corrections with identical executable Python syntax trees;
- presentation revisions with checked numerical arrays and plotted geometry;
- the rejection-cascade correction, independently regenerated from unchanged
  metadata after an omitted admission decision was found.

Original sources and selected before/after artifacts are retained to make these
claims reviewable. They are historical snapshots, not alternate current methods.
The authoritative code is under `src/` and `scripts/`; current methods are in the
chapter sources and `docs/guide/`.

No test suite certifies every scientific assertion. Independent phase annotation,
finite-record model-rejection calibration, GNSS datum harmonization and sensitivity
to retained vertical-step candidates remain explicit limitations or subsequent work.
The annotation pack is ready; no human reference labels have been invented.
